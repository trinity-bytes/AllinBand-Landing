<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" fill="none">
  <!-- Fondo circular -->
  <circle cx="16" cy="16" r="16" fill="#2563EB"/>
  
  <!-- Pulsera/Band (forma simplificada) -->
  <rect x="11" y="10" width="10" height="12" rx="2" fill="#fff" opacity="0.9"/>
  
  <!-- Punto central (sensor) -->
  <circle cx="16" cy="16" r="2" fill="#10B981"/>
  
  <!-- Líneas vitales (electrocardiograma estilizado) -->
  <path d="M13 16 L14 16 L14.5 14 L15.5 18 L16 16 L19 16" 
        stroke="#2563EB" 
        stroke-width="1.5" 
        fill="none" 
        stroke-linecap="round" 
        stroke-linejoin="round"/>
</svg>
